Bootstrap:docker
From:nfcore/base:latest

%environment
        export PYTHONNOUSERSITE=True
        export PATH="/opt/conda/bin:/usr/bin:$PATH"
        export PATH="/opt/conda/envs/jasen/bin:/usr/src/:/usr/src/cge:$PATH"
        export BASH_ENV="/conda_init.sh:$BASH_ENV"
        export TMPDIR=/tmp
        export TEMPDIR=/tmp
        export PYTHONPATH="/usr/src"

%labels
	MAINTAINER Markus Johansson <markus.h.johansson@skane.se>
	DESCRIPTION Singularity container for CMD microbiology WGS pipeline
	VERSION 4.1.5

%post
         RESFINDER_VERSION=4.1.5
         KMA_VERSION=1.3.27
         BLAST_VERSION=2.9.0

         apt-get update &&    \
         apt-get upgrade -y && \
         apt -y install build-essential make curl unzip locales libz-dev git
         apt -y install apt-utils wget python3-pip ncbi-blast+
         rm -rf /var/lib/apt/lists/*/var/lib/apt/lists/* && \
 
         ln -s /opt/conda/lib/libcrypto.so.1.1 /opt/conda/lib/libcrypto.so.1.0.0 
         locale-gen en_US.UTF-8
         localedef -i en_US -c -f UTF-8 -A /usr/share/locale/locale.alias en_US.UTF-8


         # /opt/conda/bin/conda init bash && \
         . /opt/conda/etc/profile.d/conda.sh && \
         /opt/conda/bin/conda config --set auto_activate_base true

         # /opt/conda/bin/conda create -n jasen && \
         . /opt/conda/bin/activate jasen
         pip install tabulate==0.7.7 cgecore==1.5.6 biopython gitpython python-dateutil
         # . /opt/conda/bin/deactivate

	 cd $HOME &&  \
	 git clone -b "${RESFINDER_VERSION}" --depth 1 https://bitbucket.org/genomicepidemiology/resfinder.git && \
         cd resfinder
      	 cp run_resfinder.py /usr/bin/run_resfinder.py
	 cp run_resfinder.py /usr/src/run_resfinder.py
         cp -r cge /usr/src/cge
         cp -r tests /usr/src/tests


         cd /usr/src/cge && \
	 git clone -b "${KMA_VERSION}" --depth 1  https://bitbucket.org/genomicepidemiology/kma.git && \
	 cd kma &&                                                                              \
	 make &&                                                                                              \
	 cp kma* /usr/bin
	 cp kma* /bin/

	 cd $HOME &&                                                                                                                                    \
	 wget -O blast.tar.gz "https://ftp.ncbi.nlm.nih.gov/blast/executables/blast+/${BLAST_VERSION}/ncbi-blast-${BLAST_VERSION}+-x64-linux.tar.gz" && \
	 tar -xzf blast.tar.gz && \
	 cd "ncbi-blast-${BLAST_VERSION}+/bin/" && \
	 chmod +x * && \
	 cp * /usr/bin && \
	 cd $HOME

%runscript
         # . /opt/conda/etc/profile.d/conda.sh
         # . /opt/conda/bin/activate jasen
         cd /usr/src/
         exec "$@"`
         # . /opt/conda/bin/deactivate

